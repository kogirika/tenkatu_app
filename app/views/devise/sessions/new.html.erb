<body style="isplay: flex;
    align-items: center;
    padding-top: 40px;
    padding-bottom: 40px;
    background-color: #f5f5f5;">

  <div class="container" style="width: 100%;
    max-width: 420px;
    padding: 15px;
    margin: auto;">
    <div class="text-center mb-4">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <h1 class="h2 mb-3 font-weight-normal">Tenkatu_app</h1>
      <p>あなたの転職活動を管理しよう</p>

      <div class="form-label-group">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control mb-3", placeholder:"メールアドレス" %>
      </div>

      <div class="form-label-group">
        <%= f.password_field :password, autocomplete: "current-password", class:"form-control mb-3", placeholder:"パスワード" %>
      </div>

      <div class="form-label-group">
        <%= f.submit "ログイン", class:"btn btn-lg btn-primary btn-block mb-3" %>
      </div>
      <% end %>
      <%= render "devise/shared/links" %>
      <p class="mt-5 mb-3 text-muted text-center">Created by 杉</p>
      <p class="mt-2 mb-3 text-danger text-center">※ポートフォリオ用のアプリです。<br>データの保持は保証できかねます。</p>
    </div>
  </div>
</body>
